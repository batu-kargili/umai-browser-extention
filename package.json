{
  "name": "umai-extention",
  "version": "0.1.0",
  "private": true,
  "description": "DuvarAI browser extension MVP for LLM governance on ChatGPT, Gemini, and Claude.",
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "build:watch": "node ./scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "clean": "powershell -NoProfile -Command \"if (Test-Path dist) { Remove-Item -Recurse -Force dist }\""
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.2"
  }
}
